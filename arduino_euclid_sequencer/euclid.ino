/**
 * the euclidian rythm generator
 * 
 * the implementations I found online either generate incorrect patterns 
 * or plainly didn't work alltogether so I wrote my own...
 */

uint32_t euclid(uint8_t len, uint8_t pulses, uint8_t offsets) {
  uint32_t pattern = 0;

  return pattern;
}
